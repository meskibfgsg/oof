# Nothing required! This build talks to Lanyard directly from the browser —
# no API keys, no backend. Just run `npm install && npm run dev`.
#
# (This file is only here as a placeholder; you can delete it.)
